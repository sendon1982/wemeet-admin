package org.wemeet.portal.model.gstone;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class UserInfo {
    // Define attributes here as needed (empty in your JSON, so leaving it empty)
}
